{% extends 'base.html.twig' %}

{% block title %}Rénitialisation{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <div class="form-over-background">
        <h1 class="centered-title">Demande de changement votre mot de passe</h1>
        <div class="form-standard">
            {{ form_start(requestForm) }}
            {{ form_row(requestForm.name) }}
            <div>
                <small>
                    Entrez votre Pseudo et on vous envoie un lien pour changer votre mot de passe !
                </small>
            </div>
            <button class="btn btn-primary">Faire la demande</button>
        </div>

        {{ form_end(requestForm) }}
    </div>
{% endblock %}
